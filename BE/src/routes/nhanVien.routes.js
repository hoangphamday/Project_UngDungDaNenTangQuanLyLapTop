const r=require('express').Router();const c=require('../controllers/nhanVien.controller');const v=require('../validators/nhanVien.validator');const {onlyAdmin,idHopLe}=require('./middleware');r.get('/admin/staff',...onlyAdmin,c.danhSach);r.get('/admin/staff/:id',...onlyAdmin,idHopLe,c.chiTiet);r.post('/admin/staff',...onlyAdmin,v.tao,c.tao);r.put('/admin/staff/:id',...onlyAdmin,idHopLe,v.capNhat,c.capNhat);r.patch('/admin/staff/:id/status',...onlyAdmin,idHopLe,v.trangThai,c.doiTrangThai);module.exports=r;
