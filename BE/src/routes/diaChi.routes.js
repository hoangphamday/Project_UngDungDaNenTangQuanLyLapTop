const r=require('express').Router();const c=require('../controllers/diaChi.controller');const v=require('../validators/diaChi.validator');const {customer,idHopLe}=require('./middleware');r.get('/addresses',...customer,c.danhSach);r.post('/addresses',...customer,v.tao,c.tao);r.put('/addresses/:id',...customer,idHopLe,v.capNhat,c.capNhat);r.delete('/addresses/:id',...customer,idHopLe,c.xoa);r.patch('/addresses/:id/default',...customer,idHopLe,c.datMacDinh);module.exports=r;
