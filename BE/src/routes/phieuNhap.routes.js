const r=require('express').Router();const c=require('../controllers/phieuNhap.controller');const v=require('../validators/phieuNhap.validator');const {manager,idHopLe}=require('./middleware');r.get('/admin/import-receipts',...manager,c.danhSach);r.get('/admin/import-receipts/:id',...manager,idHopLe,c.chiTiet);r.post('/admin/import-receipts',...manager,v.ghi,c.tao);r.put('/admin/import-receipts/:id',...manager,idHopLe,v.ghi,c.capNhat);r.post('/admin/import-receipts/:id/complete',...manager,idHopLe,c.hoanTat);r.post('/admin/import-receipts/:id/cancel',...manager,idHopLe,c.huy);module.exports=r;
