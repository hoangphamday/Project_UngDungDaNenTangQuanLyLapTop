const r=require('express').Router();const c=require('../controllers/hangLaptop.controller');const v=require('../validators/api.validator');const {manager,idHopLe}=require('./middleware');r.get('/brands',c.danhSach);r.get('/brands/:id',idHopLe,c.chiTiet);r.post('/brands',...manager,v.brand,c.tao);r.put('/brands/:id',...manager,idHopLe,v.brand,c.capNhat);r.patch('/brands/:id/status',...manager,idHopLe,v.booleanStatus,c.doiTrangThai);module.exports=r;
